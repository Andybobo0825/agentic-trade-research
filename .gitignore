# Secrets / local environment
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
*.mobileprovision

# Dependencies / build output
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
coverage/
.nyc_output/

# Runtime state, logs, local agent artifacts
.omx/
*.log
*.pid
*.pid.lock
/tmp/
tmp/
.cache/

# LINE / bridge local artifacts
line-bridge-responses/
authorized-users.json
trade-line-runtime.json

# OS / editor noise
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Test artifacts
junit.xml
test-results/
playwright-report/

.worktrees/

# Research data and environment. The tick archive here runs to 13G and the
# built artefacts are large enough that a single `git add -A` would try to
# commit all of it; only the code under tmf-research-agent/ belongs in git.
tmf-research-agent/data/
tmf-research-agent/phase5-holdout/
tmf-research-agent/.venv/
