# Specification

The canonical, versioned specification for this sidecar is
[`../docs/txresearch.md`](../docs/txresearch.md), currently version 1.1.0.

This forwarding document avoids maintaining a second copy whose safety rules or
phase gates could diverge from the approved source.
